Place your YOLOv11 weights here as best.pt.

The detection backend is configured to load the model from backend/models/best.pt.

Make sure this is a real `.pt` weights file, not a dataset folder or a placeholder text file. If the file size is very small (like 100 bytes), it is not a valid model.
